import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
	formGridContainer: {
		maxWidth: 550,
		marginTop: 20,
		marginLeft: 30,
	},
	textField: {
		color: "#fff",
	},

	inputLabel: {
		color: "#fff",
	},
});

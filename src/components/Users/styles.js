import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
	formGridContainer: {
		maxWidth: 500,
		marginTop: 20,
		marginLeft: 55,
	},

	textField: {
		color: "#fff",
	},

	inputLabel: {
		color: "#fff",
	},

	underline: {
		borderBottom: "3px solid #fff",
	},
});
